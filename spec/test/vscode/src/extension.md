# Test Specification: `extension.test.ts`

## Source File Under Test
`vscode/src/extension.ts`

## Test File
`vscode/test/suite/extension.test.ts`

---

## `activate`

### Happy Path — activate

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_activate_createsOutputChannelFromDeps` | `unit` | Uses deps.outputChannel when provided. | Provide a `deps` object with a mock `outputChannel` and stubs for remaining collaborators (`resolveProjectRoot` returns `'/workspace'`). Provide a fake `context` with `subscriptions` array and `extensionUri`. | `activate(context, deps)` | The mock `outputChannel` is used for logger construction; no `createOutputChannel` is called |
| `test_activate_createsOutputChannelViaDepsFactory` | `unit` | Uses deps.createOutputChannel when outputChannel is not provided. | Provide a `deps` object with a spy `createOutputChannel` (returns mock channel) but no `outputChannel` field. Provide `resolveProjectRoot` returning `'/workspace'` and other stubs. | `activate(context, deps)` | `deps.createOutputChannel` called with `'Spectra'` |
| `test_activate_createsOutputChannelViaVscode` | `unit` | Uses vscode.window.createOutputChannel when deps provides neither outputChannel nor createOutputChannel. | Provide a `deps` object with `resolveProjectRoot` returning `'/workspace'` and other stubs but no `outputChannel` or `createOutputChannel`. Stub `require("vscode")` to return a mock with `window.createOutputChannel` spy. | `activate(context, deps)` | `vscode.window.createOutputChannel` called with `'Spectra'` |
| `test_activate_logsActivationStart` | `unit` | Logs activation start before resolving project root. | Provide `deps` with mock `outputChannel` and `resolveProjectRoot` returning `'/workspace'`. Capture `outputChannel.appendLine` calls. | `activate(context, deps)` | `outputChannel.appendLine` called with a string containing `[INFO]` and `activating` before `resolveProjectRoot` is invoked |
| `test_activate_resolvesProjectRootFromDeps` | `unit` | Uses deps.resolveProjectRoot() when provided. | Provide `deps` with a spy `resolveProjectRoot` returning `'/workspace'` and other stubs. | `activate(context, deps)` | `deps.resolveProjectRoot` called exactly once |
| `test_activate_resolvesProjectRootFromProductionDefault` | `unit` | Calls ProjectRootResolver.resolve(vscode.workspace) when deps.resolveProjectRoot is not provided. | Provide `deps` with `outputChannel` (so vscode is not required for channel) but no `resolveProjectRoot`. Stub `require("vscode")` to return a mock; stub `ProjectRootResolver.resolve` to return `'/workspace'`. | `activate(context, deps)` | `ProjectRootResolver.resolve` called with `vscode.workspace` |
| `test_activate_createsSessionListController` | `unit` | Constructs SessionListController with projectRoot, logger, and controllerDeps. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs for all collaborators. Spy on `SessionListController` constructor. | `activate(context, deps)` | `SessionListController` constructed with `'/workspace'`, logger, and a deps object containing watcher/scanner/launcher/terminator/emitter factories |
| `test_activate_createsSessionDetailController` | `unit` | Constructs SessionDetailController with projectRoot, logger, and controllerDeps. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs for all collaborators. Spy on `SessionDetailController` constructor. | `activate(context, deps)` | `SessionDetailController` constructed with `'/workspace'`, logger, and a deps object containing watcher/scanner/dispatcher/emitter factories |
| `test_activate_createsViewProvider` | `unit` | Constructs SpectraViewProvider with extensionUri and logger. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Spy on `SpectraViewProvider` constructor. | `activate(context, deps)` | `SpectraViewProvider` constructed with `context.extensionUri` and logger |
| `test_activate_createsViewProviderFromDepsFactory` | `unit` | Uses deps.createViewProvider when provided. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a spy `createViewProvider` returning a mock viewProvider. | `activate(context, deps)` | `deps.createViewProvider` called; the returned mock is used for registration |
| `test_activate_registersViewProvider` | `unit` | Registers the view provider with correct viewType and options. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a spy `registerWebviewViewProvider`. | `activate(context, deps)` | `deps.registerWebviewViewProvider` called with `'spectra.chatView'`, the viewProvider instance, and `{ webviewOptions: { retainContextWhenHidden: true } }` |
| `test_activate_registersOpenPanelCommand` | `unit` | Registers the spectra.openPanel command. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a spy `registerCommand`. | `activate(context, deps)` | `deps.registerCommand` called with `'spectra.openPanel'` and a handler function |
| `test_activate_openPanelCommandHandlerIsNoOp` | `unit` | The spectra.openPanel command handler does nothing when invoked. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a stub `registerCommand` that captures the handler. Invoke the captured handler after activation. | `activate(context, deps)` then invoke captured handler | Handler returns without errors or side effects |
| `test_activate_pushesAllDisposablesToSubscriptions` | `unit` | Pushes OutputChannel, controllers, viewProvider, registration, command disposable, and subscription disposables to context.subscriptions. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs returning disposable mocks. Provide a fake `context` with an empty `subscriptions` array. | `activate(context, deps)` | `context.subscriptions` contains: OutputChannel, sessionListController, sessionDetailController, viewProvider, view provider registration disposable, command disposable, and subscription disposables |
| `test_activate_logsSuccessWithProjectRoot` | `unit` | Logs successful activation including the resolved projectRoot. | Provide `deps` with mock `outputChannel` and `resolveProjectRoot` returning `'/my/project'`. Capture `outputChannel.appendLine` calls. | `activate(context, deps)` | `outputChannel.appendLine` called with a string containing `[INFO]` and `'/my/project'` |

### Happy Path — onDidUpdate subscriptions

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_activate_sessionListOnDidUpdate_cachesStateAndShowsList` | `unit` | Caches received state and calls viewProvider.showSessionList on sessionListController update. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Configure mock `sessionListController.onDidUpdate` to accept a callback. After activation, trigger the callback with a fake state object. | Callback triggered with `state` | `viewProvider.showSessionList` called with the same `state`; state is cached internally |
| `test_activate_sessionDetailOnDidUpdate_showsDetail` | `unit` | Calls viewProvider.showSessionDetail on sessionDetailController update. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Configure mock `sessionDetailController.onDidUpdate` to accept a callback. After activation, trigger the callback with a fake detail state. | Callback triggered with `detailState` | `viewProvider.showSessionDetail` called with `detailState` |

### Error Propagation

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_activate_sessionListOnDidError_showsErrorMessage` | `unit` | Calls showErrorMessage when sessionListController fires onDidError. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a spy `showErrorMessage`. Configure mock `sessionListController.onDidError` to accept a callback. After activation, trigger the error callback with `{ message: 'scan failed' }`. | Error event with `message: 'scan failed'` | `deps.showErrorMessage` called with `'scan failed'` |
| `test_activate_sessionDetailOnDidError_showsErrorMessage` | `unit` | Calls showErrorMessage when sessionDetailController fires onDidError. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a spy `showErrorMessage`. Configure mock `sessionDetailController.onDidError` to accept a callback. After activation, trigger the error callback with `{ message: 'detail error' }`. | Error event with `message: 'detail error'` | `deps.showErrorMessage` called with `'detail error'` |

### Happy Path — onDidReceiveMessage routing

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_activate_messageRouting_navigateToDetail` | `unit` | Routes navigateToDetail to sessionDetailController.open. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Configure mock `viewProvider.onDidReceiveMessage` to accept a callback. Spy on `sessionDetailController.open`. Trigger callback with `{ command: 'navigateToDetail', sessionId: 's1', workflowName: 'wf1' }`. | Message `{ command: 'navigateToDetail', sessionId: 's1', workflowName: 'wf1' }` | `sessionDetailController.open` called with `'s1'`, `'wf1'` |
| `test_activate_messageRouting_navigateToList_withCache` | `unit` | Routes navigateToList to viewProvider.showSessionList with cached state. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. First trigger `sessionListController.onDidUpdate` with `cachedState` to populate cache. Then trigger `viewProvider.onDidReceiveMessage` with `{ command: 'navigateToList' }`. | Message `{ command: 'navigateToList' }` | `viewProvider.showSessionList` called with `cachedState` |
| `test_activate_messageRouting_navigateToList_noCacheNoOp` | `unit` | No-op when navigateToList received before first onDidUpdate. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Do NOT trigger `sessionListController.onDidUpdate`. Trigger `viewProvider.onDidReceiveMessage` with `{ command: 'navigateToList' }`. | Message `{ command: 'navigateToList' }` | `viewProvider.showSessionList` is not called |
| `test_activate_messageRouting_launchSession` | `unit` | Routes launchSession to sessionListController.launch. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Spy on `sessionListController.launch`. Trigger message `{ command: 'launchSession', workflowName: 'deploy' }`. | Message `{ command: 'launchSession', workflowName: 'deploy' }` | `sessionListController.launch` called with `'deploy'` |
| `test_activate_messageRouting_terminateSession` | `unit` | Routes terminateSession to sessionListController.terminate. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Spy on `sessionListController.terminate`. Trigger message `{ command: 'terminateSession', pid: 1234 }`. | Message `{ command: 'terminateSession', pid: 1234 }` | `sessionListController.terminate` called with `1234` |
| `test_activate_messageRouting_sendEvent` | `unit` | Routes sendEvent to sessionDetailController.sendEvent. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Spy on `sessionDetailController.sendEvent`. Trigger message `{ command: 'sendEvent', eventType: 'input', message: 'hello' }`. | Message `{ command: 'sendEvent', eventType: 'input', message: 'hello' }` | `sessionDetailController.sendEvent` called with `'input'`, `'hello'` |
| `test_activate_messageRouting_unknownCommand_logsWarning` | `unit` | Logs a warning for unrecognized webview message commands. | Provide `deps` with mock `outputChannel` and `resolveProjectRoot` returning `'/workspace'`. Capture `outputChannel.appendLine` calls. Trigger message `{ command: 'unknownCmd' }`. | Message `{ command: 'unknownCmd' }` | `outputChannel.appendLine` called with a string containing `[WARN]` and `'unknownCmd'` |

### Null / Empty Input

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_activate_projectRootUndefined_showsErrorAndReturnsEarly` | `unit` | Shows error message and returns early when projectRoot is undefined. | Provide `deps` with `resolveProjectRoot` returning `undefined` and a spy `showErrorMessage`. Provide a fake `context` with a `subscriptions` array. | `activate(context, deps)` | `deps.showErrorMessage` called with `"Spectra: No workspace folder open."`; no controllers created; no ViewProvider created; only OutputChannel pushed to `context.subscriptions` |
| `test_activate_projectRootUndefined_logsError` | `unit` | Logs the error when projectRoot is undefined. | Provide `deps` with mock `outputChannel` and `resolveProjectRoot` returning `undefined`. Capture `outputChannel.appendLine` calls. | `activate(context, deps)` | `outputChannel.appendLine` called with a string containing `[ERROR]` |
| `test_activate_projectRootUndefined_onlyOutputChannelInSubscriptions` | `unit` | Only the OutputChannel is pushed to context.subscriptions when projectRoot is undefined. | Provide `deps` with `resolveProjectRoot` returning `undefined` and a spy `showErrorMessage`. Provide a mock `outputChannel`. Provide a fake `context` with an empty `subscriptions` array. | `activate(context, deps)` | `context.subscriptions` contains only the OutputChannel |

### Mock / Dependency Interaction

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_activate_loggerWrapsOutputChannel` | `unit` | Logger adapter delegates info/warn/error to outputChannel.appendLine with severity tags. | Create a mock `outputChannel` with a spy on `appendLine`. Provide `deps` with the mock `outputChannel` and `resolveProjectRoot` returning `undefined` (so activate returns early after logging). Provide spy `showErrorMessage`. | `activate(context, deps)` | `outputChannel.appendLine` called with strings containing `[INFO]` prefix |
| `test_activate_loggerSeverityTags` | `unit` | Logger prepends [INFO], [WARN], [ERROR] for respective methods. | Create a mock `outputChannel` with a spy on `appendLine`. Provide `deps` with the mock `outputChannel` and `resolveProjectRoot` returning `'/workspace'`. After activation, trigger an unrecognized message to invoke `logger.warn`. | `activate(context, deps)` then trigger unknown message | `outputChannel.appendLine` calls include strings with `[INFO]` and `[WARN]` prefixes |
| `test_activate_terminateFromDetailPage_routesToSessionListController` | `unit` | terminateSession from detail page routes to sessionListController.terminate identically. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and stubs. Spy on `sessionListController.terminate`. Trigger message `{ command: 'terminateSession', pid: 5678 }` (simulating origin from detail page). | Message `{ command: 'terminateSession', pid: 5678 }` | `sessionListController.terminate` called with `5678` |
| `test_activate_requiresVscodeWhenNoDepsOutputChannel` | `unit` | Lazily requires vscode module when deps provides neither outputChannel nor createOutputChannel. | Provide `deps` as `{}`. Stub `require("vscode")` to return a mock vscode module. Stub `ProjectRootResolver.resolve` to return `undefined`. | `activate(context, {})` | `require` called with `"vscode"` |
| `test_activate_doesNotRequireVscodeWhenDepsProvideOutputChannel` | `unit` | Does not require vscode module when deps provides outputChannel. | Provide `deps` with mock `outputChannel`, `resolveProjectRoot` returning `undefined`, and spy `showErrorMessage`. Spy on `require`. | `activate(context, deps)` | `require` not called with `"vscode"` |
| `test_activate_depsEmptyObject_usesProductionDefaults` | `unit` | All collaborators are constructed using production defaults when deps is empty object. | Stub `require("vscode")` to return a mock vscode module. Stub production `ProjectRootResolver.resolve` to return `'/workspace'`. Spy on `SessionListController`, `SessionDetailController`, and `SpectraViewProvider` constructors. | `activate(context, {})` | All three constructors are called using production implementations; no error thrown |
| `test_activate_depsUndefined_usesProductionDefaults` | `unit` | All collaborators are constructed using production defaults when deps is undefined. | Stub `require("vscode")` to return a mock vscode module. Stub production `ProjectRootResolver.resolve` to return `'/workspace'`. Spy on `SessionListController`, `SessionDetailController`, and `SpectraViewProvider` constructors. | `activate(context)` (no second argument) | All three constructors are called using production implementations; no error thrown |
| `test_activate_depsPartial_mergesWithProductionDefaults` | `unit` | When deps provides only some fields, remaining fields use production defaults. | Provide `deps` with only `outputChannel`. Stub `require("vscode")` for remaining deps. Stub `ProjectRootResolver.resolve` to return `'/workspace'`. Spy on `SessionListController` and `SpectraViewProvider` constructors. | `activate(context, { outputChannel: mockChannel })` | `mockChannel` is used for the OutputChannel; production constructors are called for controllers and ViewProvider |
| `test_activate_usesShowErrorMessageFromDeps` | `unit` | Uses deps.showErrorMessage instead of vscode.window.showErrorMessage. | Provide `deps` with `resolveProjectRoot` returning `undefined` and a spy `showErrorMessage`. | `activate(context, deps)` | `deps.showErrorMessage` called; `vscode.window.showErrorMessage` not called |
| `test_activate_usesRegisterCommandFromDeps` | `unit` | Uses deps.registerCommand instead of vscode.commands.registerCommand. | Provide `deps` with `resolveProjectRoot` returning `'/workspace'` and a spy `registerCommand`. | `activate(context, deps)` | `deps.registerCommand` called with `'spectra.openPanel'` |

---

## `deactivate`

### Happy Path — deactivate

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `test_deactivate_isEmptyFunction` | `unit` | deactivate does nothing — cleanup is handled by context.subscriptions. | None | (no arguments) | Function returns `undefined`; no errors thrown |
