# Test Specification: `getWebviewContent.test.ts`

## Source File Under Test
`vscode/src/views/getWebviewContent.ts`

## Test File
`vscode/test/suite/getWebviewContent.test.ts`

---

## `getWebviewContent`

### Happy Path — getWebviewContent

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `should return a valid HTML5 document` | `unit` | Output starts with DOCTYPE and contains html/head/body tags. | Create a stub `vscode.Webview` with `cspSource` returning `'https://test.csp'`. Create a stub `vscode.Uri` for extensionUri. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string starts with `<!DOCTYPE html>` and contains `<html`, `<head`, `<body` tags |
| `should include CSP meta tag with nonce-gated style-src and script-src without font-src` | `unit` | CSP meta tag is correctly formed with required directives and no font-src. | Create a stub `vscode.Webview` with `cspSource` returning `'https://test.csp'`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains a `<meta` tag with `default-src 'none'`, `style-src 'nonce-`, `script-src 'nonce-`; does NOT contain `font-src` |
| `should include a style block with matching nonce` | `unit` | Style tag uses the same nonce as CSP. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains exactly one `<style nonce="..."` where the nonce value matches the nonce in the CSP meta tag |
| `should include a script block with matching nonce` | `unit` | Script tag uses the same nonce as CSP. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains exactly one `<script nonce="..."` where the nonce value matches the nonce in the CSP meta tag |
| `should generate a unique nonce per invocation` | `unit` | Nonce changes between calls. | Create a stub `vscode.Webview` with `cspSource`. | Call `getWebviewContent(stubWebview, stubExtensionUri)` twice | The nonce extracted from the first result differs from the nonce extracted from the second result |
| `should contain not-initialized page element` | `unit` | DOM contains page-not-initialized. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="page-not-initialized"` |
| `should contain spectra init message in not-initialized page` | `unit` | Not-initialized page displays initialization instruction. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains text `spectra init` within the not-initialized page section |
| `should contain header element with text Spectra` | `unit` | Header displays application name. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains a header element with the text content `Spectra` |
| `should contain sessions list page element` | `unit` | DOM contains page-sessions. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="page-sessions"` |
| `should contain session detail page element` | `unit` | DOM contains page-detail. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="page-detail"` |
| `should contain workflow-select dropdown` | `unit` | Sessions page has the workflow dropdown. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains a `<select` element with `id="workflow-select"` |
| `should contain Run button` | `unit` | Sessions page has the Run button. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="btn-run"` |
| `should contain session-list container` | `unit` | Sessions page has the session list container. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="session-list"` |
| `should contain back button with inline SVG chevron-left icon` | `unit` | Detail page has the back button rendered as a 28×28px icon-only button with inline SVG. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="btn-back"` that contains an `<svg` element. No text label is present in the button element. Does not reference any `codicon` class. |
| `should style back button as 28x28px inline-flex square` | `unit` | Back button uses fixed 28×28 sizing with inline-flex centering. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#btn-back` includes `display: inline-flex`, `align-items: center`, `justify-content: center`, `width: 28px`, `height: 28px`, and `flex-shrink: 0` |
| `should use currentColor for SVG stroke in back button` | `unit` | The SVG chevron inherits text color from the theme. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | The `<svg` element within `#btn-back` uses `currentColor` for its `stroke` attribute |
| `should contain event-list container on detail page` | `unit` | Detail page has the event list. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="event-list"` |
| `should contain event-type-select dropdown on detail page` | `unit` | Detail page has the event type dropdown. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains a `<select` element with `id="event-type-select"` |
| `should contain event-message-input textarea on detail page` | `unit` | Detail page has the textarea for message input. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains a `<textarea` element with `id="event-message-input"` and `rows="3"` attribute |
| `should contain Send button on detail page` | `unit` | Detail page has the Send button. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains an element with `id="btn-send"` |
| `should include acquireVsCodeApi call in script` | `unit` | Client JS acquires the API. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains `acquireVsCodeApi()` |
| `should include message event listener in script` | `unit` | Client JS listens for messages. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains `addEventListener('message'` or `addEventListener("message"` |
| `should not contain inline event handlers` | `unit` | No onclick attributes in HTML. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string does not contain `onclick=`, `onsubmit=`, `onchange=`, or `onkeydown=` |
| `should not use eval in script` | `unit` | No eval usage. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string does not contain `eval(` |
| `should have all three pages in DOM simultaneously` | `unit` | All pages coexist for CSS visibility toggling. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string contains `id="page-not-initialized"`, `id="page-sessions"`, and `id="page-detail"` all present in the same document |
| `should define hidden CSS class with display-none-important` | `unit` | The `.hidden` class overrides display to hide pages. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS contains a `.hidden` rule that applies `display: none !important` |
| `should apply flex layout to workflow dropdown row` | `unit` | The row containing the dropdown and Run button uses flex layout. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS contains `display: flex` and `align-items: center` and `gap: 8px` applied to the workflow row container |
| `should apply flex-1 and min-width-0 to workflow-select` | `unit` | The dropdown fills remaining space and shrinks gracefully. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#workflow-select` (or its container selector) includes `flex: 1` and `min-width: 0` |
| `should apply flex-shrink-0 to Run button` | `unit` | The Run button does not shrink. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#btn-run` (or its container selector) includes `flex-shrink: 0` |
| `should not reference any external CDN or font URLs` | `unit` | No external resources are loaded; all content is inline. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string does not contain any `https://` or `http://` URLs in `<link`, `@font-face`, or `@import` rules (CSP meta tag `cspSource` references are excluded from this check) |
| `should render stop button as circular icon with pulse animation` | `unit` | Stop button is a 20×20px circle with 8×8px inner square and CSS pulse animation. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS contains a `@keyframes pulse` rule that animates `transform: scale` between 1.0 and 1.15 with a 2s period. The stop button styling defines a circular shape (border-radius 50% or equivalent for 20×20px) and the inner square (8×8px). No codicon class is referenced for the stop button. |
| `should apply detail page full-height flex column layout` | `unit` | The detail page container uses a flex column layout pinning controls to the bottom. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#page-detail` (or its content container) includes `display: flex`, `flex-direction: column`, and `height: 100%` |
| `should apply flex-1 and overflow-y-auto to event-list container` | `unit` | The event history fills remaining vertical space and scrolls. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#event-list` includes `flex: 1` and `overflow-y: auto` |
| `should apply chat bubble styling to event entries in embedded CSS` | `unit` | CSS includes chat bubble rules with correct alignment and styling. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS includes bubble styles: `border-radius: 12px`, `padding` of `8px 12px`, `max-width` of `80%`, and two distinct background color rules (one using `--vscode-editorWidget-background` for left-aligned, one using `--vscode-button-background` for right-aligned) |
| `should use textContent for rendering event Type and Message in embedded JS` | `unit` | Embedded script uses textContent for event rendering to prevent XSS. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block uses `textContent` (not `innerHTML`) when assigning event `Type` labels and `Message` text to DOM elements |
| `should include auto-scroll logic in embedded JS` | `unit` | Embedded script scrolls event-list to bottom after rebuilding events. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block contains logic setting `scrollTop = scrollHeight` (or equivalent) on the `event-list` container after rebuilding event entries |
| `should apply word-wrap styles to chat bubble message text` | `unit` | Chat bubble CSS ensures long text wraps and line breaks are preserved. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for chat bubble message text includes `word-wrap: break-word` (or `overflow-wrap: break-word`) and `white-space: pre-wrap` |
| `should render event type label above bubble in muted color` | `unit` | Event Type label is displayed above the chat bubble with secondary color styling. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for the event type label includes `color` referencing `--vscode-descriptionForeground` and `font-size` of `11px` |
| `should apply flex layout to event controls first row` | `unit` | The event-type dropdown and Send button row uses flex layout. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for the event controls row includes `display: flex`, `align-items: center`, and `gap: 8px` |
| `should apply flex-1 and min-width-0 to event-type-select` | `unit` | The event type dropdown fills remaining space. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#event-type-select` includes `flex: 1` and `min-width: 0` |
| `should apply flex-shrink-0 to Send button` | `unit` | The Send button does not shrink. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#btn-send` includes `flex-shrink: 0` |
| `should configure textarea with resize-vertical and correct dimensions` | `unit` | The event message textarea is resizable vertically and has 3 rows. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#event-message-input` includes `resize: vertical` and `width: 100%`. The textarea element has `rows="3"`. |
| `should include pulse animation keyframes using CSS not JS timers` | `unit` | Pulse animation is declared via CSS @keyframes, not JavaScript setInterval/setTimeout. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS contains `@keyframes pulse` with `transform` properties. The script block does not contain `setInterval` or `setTimeout` calls associated with the pulse animation (cooldown setTimeout is separate and acceptable). |
| `should apply back button hover style with border-radius` | `unit` | Back button has hover background highlight with rounded corners. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for `#btn-back` hover state references `--vscode-toolbar-hoverBackground` and includes `border-radius: 4px` |
| `should apply stop button hover style with paused animation` | `unit` | Stop button hover increases opacity and pauses animation. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned CSS for the stop button hover state includes increased opacity (40%) and `animation-play-state: paused` |
| `should use hidden class toggling for page switching in showNotInitialized handler` | `unit` | The showNotInitialized message handler adds .hidden to other pages and removes it from page-not-initialized. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block contains logic that on receiving `showNotInitialized` message: adds `hidden` class to `page-sessions` and `page-detail`, removes `hidden` class from `page-not-initialized` (uses `classList.add`/`classList.remove`) |
| `should use hidden class toggling for page switching in showSessions handler` | `unit` | The showSessions message handler adds .hidden to other pages and removes it from page-sessions. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block contains logic that on receiving `showSessions` message: adds `hidden` class to `page-detail` and `page-not-initialized`, removes `hidden` class from `page-sessions` (uses `classList.add`/`classList.remove`) |
| `should use hidden class toggling for page switching in showDetail handler` | `unit` | The showDetail message handler adds .hidden to other pages and removes it from page-detail. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block contains logic that on receiving `showDetail` message: adds `hidden` class to `page-sessions` and `page-not-initialized`, removes `hidden` class from `page-detail` (uses `classList.add`/`classList.remove`) |
| `should align event bubbles based on EmittedBy equals entryNode` | `unit` | Embedded JS uses EmittedBy field compared to entryNode for chat bubble alignment. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block contains alignment logic comparing `EmittedBy` (capitalized) to `entryNode` variable to determine right-aligned vs left-aligned class assignment |
| `should store entryNode from showDetail state payload` | `unit` | Embedded JS stores entryNode in a module-level variable from the showDetail message. | Create a stub `vscode.Webview` with `cspSource`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned script block contains logic that on receiving `showDetail` message, stores `state.entryNode` in a variable accessible to the event rendering logic |

### Mock / Dependency Interaction

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `should access webview.cspSource` | `unit` | CSP source is read from webview. | Create a stub `vscode.Webview` with `cspSource` as a sinon property spy returning `'https://csp.test'`. | `getWebviewContent(stubWebview, stubExtensionUri)` | `cspSource` getter was accessed at least once |

### Null / Empty Input

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `should produce valid HTML when cspSource is empty string` | `unit` | Empty cspSource does not break generation. | Create a stub `vscode.Webview` with `cspSource` returning `''`. | `getWebviewContent(stubWebview, stubExtensionUri)` | Returned string starts with `<!DOCTYPE html>` and contains CSP meta tag with nonce-based `style-src` and `script-src` directives; does NOT contain `font-src` |

### Idempotency

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `should produce structurally consistent output on repeated calls` | `unit` | Same structure each time (aside from nonce). | Create a stub `vscode.Webview` with `cspSource`. | Call `getWebviewContent(stubWebview, stubExtensionUri)` twice | Both results contain the same set of element IDs (`page-not-initialized`, `page-sessions`, `page-detail`, `btn-run`, `btn-back`, `btn-send`, `session-list`, `event-list`, `workflow-select`, `event-type-select`, `event-message-input`), both contain a `.hidden` CSS class rule, and both contain an inline SVG in the back button |
