# Test Specification: `event_processor_test.go`

## Source File Under Test

`runtime/event_processor.go`

## Test File

`runtime/event_processor_test.go`

---

## `EventProcessor`

### Happy Path — Construction

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `TestNewEventProcessor_ValidDeps` | `unit` | Constructs EventProcessor with all valid dependencies. | Create mock PersistentSession, mock WorkflowDefinition, mock TransitionToNode, and a buffered TerminationNotifier channel (cap >= 2). | `NewEventProcessor(persistentSession, workflowDef, transitionToNode, terminationNotifier)` | Returns non-nil `*EventProcessor`; no panic |

### Happy Path — ProcessEvent

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `TestEventProcessor_ProcessEvent_RegularTransition` | `unit` | Processes event and performs regular transition successfully. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition: node `"nodeA"` exists. Stub `ValidateClaudeSessionID` returns nil. Stub `TransitionEvaluator.EvaluateTransition()` returns `(transition{ToNode:"nodeB"}, false)`. Mock TransitionToNode: `Transition("nodeB", "hello")` returns nil. Create RuntimeMessage with payload `{"eventType":"msg_sent","message":"hello","payload":{}}`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `SuccessResponse("event 'msg_sent' processed successfully \| session=sess-uuid \| currentState=nodeB \| sessionStatus=running")` |
| `TestEventProcessor_ProcessEvent_ExitTransition` | `unit` | Processes event with exit transition and completes session. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil, `Done()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition: node `"nodeA"` exists. Stub `ValidateClaudeSessionID` returns nil. Stub `TransitionEvaluator.EvaluateTransition()` returns `(transition{ToNode:"exitNode"}, true)`. Mock TransitionToNode: `Transition("exitNode", "done")` returns nil. Create RuntimeMessage with payload `{"eventType":"task_done","message":"done","payload":{}}`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `SuccessResponse("event 'task_done' processed successfully \| session=sess-uuid \| currentState=exitNode \| sessionStatus=completed")`; `Done(terminationNotifier)` called once |

### Mock / Dependency Interaction

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `TestEventProcessor_ProcessEvent_CallsValidateClaudeSessionID` | `unit` | Invokes ValidateClaudeSessionID with correct arguments before recording event. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` to capture arguments and return nil. Mock remaining dependencies for success path. Create RuntimeMessage with `ClaudeSessionID()="cs-789"`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | `ValidateClaudeSessionID` called once with (persistentSession, currentNode, `"cs-789"`) |
| `TestEventProcessor_ProcessEvent_EventEntityConstruction` | `unit` | Constructs Event entity with correct fields including auto-assigned EmittedBy. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `ID` returns `"sess-uuid"`. Mock `UpdateEventHistorySafe()` captures event arg, returns nil. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator returns valid transition. Mock TransitionToNode returns nil. Create RuntimeMessage with payload `{"eventType":"data_ready","message":"msg1","payload":{"key":"val"}}`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | `UpdateEventHistorySafe()` called with Event having: non-empty UUID, `Type="data_ready"`, `Message="msg1"`, `Payload={"key":"val"}`, `EmittedBy="nodeA"`, `EmittedAt` as POSIX timestamp, `SessionID="sess-uuid"` |
| `TestEventProcessor_ProcessEvent_TransitionEvaluatorCalledCorrectly` | `unit` | Calls TransitionEvaluator with workflowDef, current node name, and event type. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeX"`, `UpdateEventHistorySafe()` returns nil. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator to capture args. Mock TransitionToNode returns nil. Create RuntimeMessage with payload `{"eventType":"ev_type","message":"m"}`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | `TransitionEvaluator.EvaluateTransition` called with (workflowDef, `"nodeX"`, `"ev_type"`) |
| `TestEventProcessor_ProcessEvent_TransitionToNodeCalledCorrectly` | `unit` | Calls TransitionToNode.Transition with target node name and event message. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator returns `(transition{ToNode:"nodeB"}, false)`. Mock TransitionToNode to capture args, returns nil. Create RuntimeMessage with payload `{"eventType":"go","message":"forward"}`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | `TransitionToNode.Transition` called once with (`"nodeB"`, `"forward"`) |

### Error Propagation

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `TestEventProcessor_ProcessEvent_SessionNotRunning_Initializing` | `unit` | Returns error response when session status is "initializing". | Mock PersistentSession: `GetStatusSafe()` returns `"initializing"`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("session not ready: status is 'initializing'")`; no event recorded |
| `TestEventProcessor_ProcessEvent_SessionNotRunning_Completed` | `unit` | Returns error response when session status is "completed". | Mock PersistentSession: `GetStatusSafe()` returns `"completed"`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("session not ready: status is 'completed'")`; no event recorded |
| `TestEventProcessor_ProcessEvent_SessionNotRunning_Failed` | `unit` | Returns error response when session status is "failed". | Mock PersistentSession: `GetStatusSafe()` returns `"failed"`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("session not ready: status is 'failed'")`; no event recorded |
| `TestEventProcessor_ProcessEvent_ClaudeSessionIDValidationFails` | `unit` | Returns error response when ValidateClaudeSessionID fails. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns `errors.New("claude session not found")`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("claude session not found")`; `UpdateEventHistorySafe` not called |
| `TestEventProcessor_ProcessEvent_InvalidPayloadMissingEventType` | `unit` | Returns error response when payload is missing eventType field. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Create RuntimeMessage with payload `{"message":"hi"}` (no eventType). | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("invalid event payload: missing eventType")` |
| `TestEventProcessor_ProcessEvent_EventRecordingFails` | `unit` | Fails session with RuntimeError when UpdateEventHistorySafe returns error. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns `errors.New("validation error")`, `Fail()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("failed to record event: validation error")`; `Fail()` called with RuntimeError having `Issuer="EventProcessor"`, `Message="failed to record event"`, `FailingState="nodeA"` |
| `TestEventProcessor_ProcessEvent_NoMatchingTransition` | `unit` | Returns error response when no transition matches the event type. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator returns `(nil, false)`. Create RuntimeMessage with payload `{"eventType":"unknown_ev","message":"x"}`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("no transition found for event 'unknown_ev' from node 'nodeA'")`; session remains running; `Fail()` not called |
| `TestEventProcessor_ProcessEvent_TransitionToNodeFails` | `unit` | Fails session with RuntimeError when TransitionToNode returns error. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil, `Fail()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator returns valid transition. Mock TransitionToNode returns `errors.New("agent invoke failed")`. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("transition failed: agent invoke failed")`; `Fail()` called with RuntimeError having `Issuer="EventProcessor"`, `Message="transition failed"`, `FailingState="nodeA"` |
| `TestEventProcessor_ProcessEvent_DoneFailsAfterExitTransition` | `unit` | Fails session with RuntimeError when PersistentSession.Done returns error after exit transition. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil, `Done()` returns `errors.New("already completed")`, `Fail()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator returns `(transition{ToNode:"exitNode"}, true)`. Mock TransitionToNode returns nil. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("failed to complete session: already completed")`; `Fail()` called with RuntimeError having `Issuer="EventProcessor"`, `Message="failed to complete session"`, `FailingState="exitNode"` |
| `TestEventProcessor_ProcessEvent_NodeNotFound` | `unit` | Returns error response when current node is not in workflow definition. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"ghost"`. Mock WorkflowDefinition: `Nodes()` returns no node matching `"ghost"`. Stub `ValidateClaudeSessionID` returns nil. | `ep.ProcessEvent("sess-uuid", runtimeMessage)` | Returns `ErrorResponse("current node 'ghost' not found in workflow definition")` |

### Concurrent Behaviour

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `TestEventProcessor_ProcessEvent_ConcurrentEventsSerialize` | `race` | Multiple concurrent events are serialized by PersistentSession's internal lock. | Mock PersistentSession: `GetStatusSafe()` returns `"running"`, `GetCurrentStateSafe()` returns `"nodeA"`, `UpdateEventHistorySafe()` returns nil, `ID` returns `"sess-uuid"`. Mock WorkflowDefinition with matching node. Stub `ValidateClaudeSessionID` returns nil. Stub TransitionEvaluator returns valid transition. Mock TransitionToNode returns nil. Create two valid RuntimeMessages with different event types. | Call `ep.ProcessEvent(...)` concurrently from two goroutines. | Both calls complete without data race; each returns a RuntimeResponse |
