# Conventions

## Language & Toolchain

Specify the primary programming language(s), version requirements, and any required build tools or linters.

## Naming

Define naming conventions for different code elements (packages, files, functions, variables, constants, interfaces, tests, etc.). Use a table format if helpful.

| Kind | Style | Example |
|---|---|---|
| [Element Type] | [Convention] | `[example]` |
| [Element Type] | [Convention] | `[example]` |
| [Element Type] | [Convention] | `[example]` |

## Code Location

Define the directory structure and placement rules for different types of code (production code, unit tests, integration tests, fixtures, etc.).

## Error Handling

Specify how errors should be handled in the codebase (error wrapping, sentinel errors, panic policies, etc.).

## Testing

Define testing frameworks, test file naming conventions, preferred testing patterns, and mocking strategies.

## Imports

Specify how imports should be organized (order, grouping, separation rules).

## Code Style

Specify additional code style preferences (variable naming scope, return patterns, parameter ordering, visibility rules, interface design, etc.).
